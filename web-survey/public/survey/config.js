// Production survey API (Cloudflare Worker).
// Local dev may override via dev-server injected header or browser console.
window.SURVEY_API_BASE = "https://donanimoperasyonservis-survey.workers.dev";
