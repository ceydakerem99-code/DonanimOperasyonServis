// Copy to SurveyProductionSecrets.xcconfig (gitignored) and set the same
// value configured in Firebase:
//   firebase functions:secrets:set SURVEY_TOKEN_SECRET
SURVEY_TOKEN_SECRET = replace-with-production-secret
