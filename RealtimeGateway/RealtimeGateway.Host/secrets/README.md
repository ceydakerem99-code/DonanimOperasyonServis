# Place a Firebase service-account JSON for project `donanimoperasyonservis` here:
#
#   cp /path/to/downloaded-service-account.json firebase-service-account.json
#
# Or set GOOGLE_APPLICATION_CREDENTIALS to an absolute path instead.
#
# Never commit the real JSON file.
